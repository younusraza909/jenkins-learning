pipeline{
    agent any

    stages{
        stage("build jar"){
            script{
                echo "Building Jar File"
            }
        }

        stage("build image"){
            script{
                echo "Building Docker Image"
            }
        }

        stage("deploy build"){
            script{
                echo "Deploying Build"
            }
        }
    }
}